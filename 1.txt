• 已改好，抓取和放置两处发 capture=1 的逻辑都做了同样处理：

  - 发触发帧前，把包内底盘 x/y 从“视觉算出的目标位置”改写为“下位机当前回传位置”（src.py:2000、
    src.py:2686）；

  - 夹爪伸长量保持原来的目标值不变，动作本身不受影响；
  - capture=0 的跟踪帧不受影响，仍然发视觉目标；
  - README 里补了一句协议约定（README.md:436），方便和固件侧对齐。

  改完已通过 python3 -m py_compile 语法检查。

  真机验证时直接看日志：capture=1 那一行的“底盘目标位置”应该变成和下位机回传位置一致，例如：

  [放置] capture=1 底盘目标位置=(-1184,-1940)mm | 下位机回传=(-1184,-1940)mm ...

  如果固件那边也能做到“capture=1 时忽略底盘字段、只在本位置执行动作”，效果会最干净——上位机这侧已
  经把残余位移去掉了，固件再忽略底盘字段，理论上就不会再有任何微动。

  › 当我运行代码时先识别打开串口，若串口识别并打开成功，则进行后面的流程