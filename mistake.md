# mistake.md —— 问题与踩坑记录

## 2026-08-23 抓取“没识别稳就突然抓取”

### 现象

- 托盘阶段的物块是静止的，小车到位后识别一小会就发 `capture=1`；
- 画面上识别圈还在抖动/闪烁，但抓取已经触发。

### 原因

1. `detection.stability.max_pixel_move=20` 太松：
   相邻帧圆心位移小于 20px（约 3mm）都算“稳定”，
   画面明显抖动时稳定计数仍会继续累积；
2. `BlockDetector.on_miss()` 只清零颜色计数、保留位置计数：
   识别闪烁/中断后，位置稳定数会带着旧值继续攒，
   攒够 35 帧就触发抓取，看起来像“没有识别稳”。

### 已做的处理

- `config.yaml`：`max_pixel_move` 20 → 6（约 1mm），必须真正停稳才累计；
- `felling_color.py`：`on_miss()` 同时清零位置计数与上一帧中心，
  位置稳定必须是连续不间断的识别；
- 颜色稳定阈值保持 5 帧、位置阈值保持 35 帧。

### 待确认

- [ ] 真机复测：抓取区/托盘区识别圈不再抖动闪烁后才会发 `capture=1`；
- [ ] 若抓取区物块较快（>180px/s @30fps）导致一直攒不够稳定数，
      可把 `max_pixel_move` 适当回调（如 8~10px）。

## 2026-08-23 托盘阶段目标颜色突然跳变（蓝→黄），且没抓完就“完成”

### 现象

- 放置完成后进入托盘阶段，第一个托盘（蓝色物块）开始识别；
- 识别一会后画面目标突然变成黄色（下一遍抓取顺序的第一个颜色）；
- `placement_order.log` 出现“当前抓取对象: 托盘3 颜色=3”后紧跟“完成”，
  中间没有任何托盘 1/2 的记录，说明托盘阶段被提前跳过。

### 原因

`src.py` 托盘阶段处理 `arrived` 上升沿时：

```python
if tray_pending_digit is not None:
    pass          # 本意是“上一托盘还没抓完，忽略这次 arrived”
elif tray_plan:
    ...
```

`pass` 之后没有 `continue`，执行流会直接掉进下面
“所有托盘已抓完，收到 arrived 说明最后一个托盘抓取已结束”的分支，
把 `tray_phase_active` 置 False、清空 `tray_plan` 并调用
`advance_after_placement_cycle()` 提前进入下一遍。

下位机在识别/对准过程中 `arrived` 短暂 1→0→1（例如小车微调位移）就会触发，
所以表现为“识别一会后目标颜色突然跳变”。

### 已做的处理

- `pass` 改为 `continue`：上一托盘还没抓完时，`arrived` 上升沿整帧忽略，
  不再掉进“所有托盘已抓完”分支；
- 只有 `tray_pending_digit is None` 且 `tray_plan` 为空时的 `arrived`
  上升沿才会结束托盘阶段。

### 待确认

- [ ] 真机复测托盘阶段：蓝色（托盘3）抓完后再切到黄色（托盘1），
      不再中途跳变；日志应依次出现三个托盘的“当前抓取对象”记录再“完成”。

## 2026-08-16 像素→mm 换算出现几十米级移动量

### 现象

- 抓取日志中出现：
  `底盘移动量=(+24874,+48503)mm`
- 明显不是真实底盘应该执行的移动量。

### 已确认：显示缩放不影响坐标换算

- `show_detection()` 只缩放 `cv2.imshow` 的显示窗口；
- 检测、KF 滤波、`pixel_to_camera()` 使用的都是原始相机帧；
- 因此 `DISPLAY_MAX_WIDTH/HEIGHT`（显示缩放）对原始像素和像素→mm 换算**没有影响**；
- 像素→mm 标定也不是在显示缩放之后调的，二者无关。

### 真正原因（大概率）

- `transformer.py` 的内参基准是 `1280x720`；
- 而 `common_camera.py` 里请求的默认分辨率是 `640x480`；
- 如果实际出图是 `640x480`，仍按 720p 的主点/焦距换算，会把靠近画面下方的物块
  算成几十米远，从而产生 `+24874,+48503` 这类离谱移动量。
- 另一种可能：物块在画面中的高度接近主点行（`v ≈ cy`），`delta_v` 接近 0，
  前方距离也会被放大到极大。

### 已做的处理

1. `transformer.pixel_to_camera()`：按当前实际画面分辨率自动缩放内参
   （标定内参已改为 `640x480`，与检测相机实际出图一致，缩放系数为 1；
   缩放仅用于其他分辨率时的近似处理）；
2. `src.py` 新增指令范围保护：
   - `MAX_CHASSIS_CMD_MM = 2000`（单轴移动量超过 ±2m 视为异常）；
   - `MAX_GRIPPER_MM = 400`（夹爪伸长量超过 40cm 视为异常）；
   - 越界时沿用上一帧有效指令，不再把离谱值发下去。

### 待确认 / 后续注意

- [x] 确认物块检测相机实际出图分辨率为 `640x480`；
  - 已用 `640x480` 重新标定，`camera_calibration.json` 与 `transformer.py`
    内参均已按 `640x480` 写入，不再需要 1280→640 缩放；
  - 若更换相机或出图分辨率变化，请用实际分辨率重新标定。
- [ ] 如果日志频繁出现 `[保护] 指令越界`，说明坐标换算仍有问题，
      不要只依赖范围保护，应重新检查内参、相机安装高度/俯仰角、物块位置。
- [ ] 物块尽量保持在主点下方且不要紧贴主点行，否则前方距离容易失真。
- [ ] `px_per_mm = 0.5` 仍需用 `calibrate_px_per_mm.py` 在物块实际工作距离处实测。
- [ ] `last_valid_mm` 初始为 `(0,0,0)`，在第一条有效指令出现前，
      异常时兜底会发 `0`，效果等同停车/空闲，需要在真机上验证是否符合预期。

## 2026-08-16 底盘移动量变成 0

### 现象

- 抓取日志出现：
  `底盘移动量=(+0,+0)mm 夹爪伸长量=0mm`
- 但摄像头画面里物料中心并不在图像中心，理论上不应全为 0。

### 原因分析

- `0,0,0` 不是显示缩放造成的，也不是 `px_per_mm` 造成的；
- 更可能来自两条路径：
  1. `pixel_to_camera()` 返回 `None`（例如物块出现在主点上方/同一行，
     `delta_v <= 0`），代码只能沿用 `last_valid_mm`，初始就是 `(0,0,0)`；
  2. 坐标换算结果越界被 `sanitize_protocol_mm()` 拦截，同样沿用
     `last_valid_mm`，初始为 `(0,0,0)`。

### 已加的诊断日志

- `[transformer] 坐标无效: 目标(u,v) @ WxH, 主点行 cy=..., delta_v=...`
  → 说明物块不在主点下方，当前“相机光轴水平”的模型不成立；
- `[坐标无效] 抓取/放置: ...` → 像素坐标换算失败，沿用上一帧指令；
- `[命令全0] ... 相机坐标=...` → 像素坐标有效但转换结果本身是 0，
  需要检查相机标定/安装角度；
- `[保护] 指令越界 ...` → 数值异常被范围保护拦下。

### 待确认

- [ ] 相机实际是否俯仰安装（光轴是否水平）；
- [ ] 物料在画面中的行坐标是否高于主点行；
- [ ] 检测相机实际分辨率与内参基准是否一致。

## 2026-08-16 确认：相机向下俯拍导致坐标换算失败

### 现象

- `validate_transform.py` 点击物块 `(707,217)` @ `1280x720`：
  `delta_v=-163.2`，物块出现在主点行 `cy=377.9` 上方；
- 原模型要求物块必须在主点下方，因此直接返回 `None`，底盘/夹爪指令变成 0。

### 结论

- 相机实际是向下俯拍安装的，不是光轴水平；
- 俯拍时地面物块可以出现在主点上方，原来的 `delta_v > 0` 判断不成立。

### 已做的处理

- `transformer.pixel_to_camera()` 增加俯仰角支持：
  `CAMERA_PITCH_DEG`（正=向下）；
- `validate_transform.py` 增加 `--pitch-deg`，可现场试角度；
- `transformer.estimate_pitch_deg()`：按一次实测水平前方距离反推俯仰角；
- `validate_transform.py` 按 `P` 可自动给出建议 `CAMERA_PITCH_DEG`；
- 示例：`(707,217)` 在俯仰角约 40~50° 时可换算出 40~60cm 的合理前方距离。
- 现场确认：相机光轴垂直于地面（垂直向下安装），
  `transformer.CAMERA_PITCH_DEG` 已设为 `90.0`。
### 待办

- [x] 在真机上按 `P` 输入一次实测前方距离，得到精确俯仰角（结果：垂直向下 ≈ 90°）；
- [x] 把试好的角度写回 `transformer.CAMERA_PITCH_DEG`（当前为 `90.0`）；
- [ ] 若角度接近 0 但仍失败，再检查相机是否侧倾/偏航或物块不在同一地面高度。
